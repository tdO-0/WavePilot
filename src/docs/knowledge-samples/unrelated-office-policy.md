# 办公室访客登记制度（无关负样例）

访客进入办公区域前应在前台登记姓名、访问对象与预计离开时间，并佩戴临时访客证。接待人应陪同访客进入受控区域，离开时归还访客证。

本文不包含极化码、误码率、Monte Carlo、MATLAB 或通信仿真实验知识。它用于验证带 `documentType` 和 `experimentType` metadata 的过滤检索不会把业务无关内容混入实验上下文。
