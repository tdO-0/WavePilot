# polar-k-integration-fixture-v1

这是 Phase 4 的能量阈值 MATLAB 集成 fixture，只验证真实进程启动、超时、取消、日志以及 Artifact 链路。

`mock=false` 只表示 MATLAB 进程真实运行；`algorithmValidated=false`。该 fixture 不执行真实极化码编码或盲识别，不得作为极化码科研结果或算法性能证据。
